/*
 hello - This is a
multi-line comment 
*/

//hello -  This is a in-line comment 

/* eight different data types which are undefined, null, boolean, string, symbol, bigint, number, and object.
Use the var keyword to create a variable called myName. ---  */
var myName;
// Assign the value 7 to variable a.
var a;
a = 7;
//Assign the contents of a to variable b.
var a;
a = 7;
var b;
b = a ;

//Initialize the three variables a, b, and c with 5, 10, and "I am a" respectively so that they will not be undefined.

var a = 5;
var b = 10;
var c = "I am a";


a = a + 1;
b = b + 5;
c = c + " String!";

//So unlike var, when you use let, a variable with the same name can only be declared once.
// They are a constant value, which means that once a variable is assigned with const, it cannot be reassigned

//i++; is the equivalent of i = i + 1;
//i--; is the equivalent of i = i - 1;

//Decimal numbers are sometimes referred to as floating point numbers or floats.

let myVar = 1;
myVar += 5;
console.log(myVar);
//6 would be displayed in the console.

//myVar = myVar - 5; will subtract 5 from myVar. same with *= and /=

//const sampleStr = "Alan said, \"Peter is learning JavaScript\"."; 